import { connect } from 'react-redux' /*connect to store*/
import Header from '../components/Header/Header'; /*import component in which we want to path state and action*/



export default connect()(Header);